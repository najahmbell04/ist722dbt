version: 2

models:
  - name: fact_sales
    description: Transaction fact table. One row per line item ordered. Includes product data.
    columns:
      - name: orderid
        description: Source key used for drill through
        tests:
          - not_null

      - name: customerkey
        description: Dimension key for dim_customer

      - name: employeekey
        description: Dimension key for dim_employee

      - name: orderdatekey
        description: Dimension key for dim_date

      - name: productkey
        description: Dimension key for dim_product

      - name: quantity
        description: Quantity of product on the order
        tests:
          - not_null

      - name: extendedpriceamount
        description: quantity * unit price

      - name: discountamount
        description: extended price * discount

      - name: soldamount
        description: extended price - discount amount